spring:
  jpa:
  	database-platform: org.hibernate.dialect.H2Dialect
  	database: H2
  	pen-in-view: false
	show-sql: false
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:mytestdb;MODE=MySQL
    username: pipe
    password: pipe
    driver-class-name: org.h2.Driver